Effect: Allow
Action:
  - ssm:GetParameter
Resource:
  - !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/ALGOLIA_APP_ID"
  - !Sub "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/ALGOLIA_API_KEY"
